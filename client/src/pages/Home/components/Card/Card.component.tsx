import React from 'react';
import './Card.styles.scss';

const Card: React.FC = () => {
    const baseClass = 'pm-card';
    return (
        <div className={baseClass} />
    );
};

export default Card;
